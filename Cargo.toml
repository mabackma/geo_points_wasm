[package]
name = "geo_points_wasm"
version = "0.1.0"
edition = "2021"

[dependencies]
geo_points = { path = "../geo_points" }
wasm-bindgen =  "0.2.93"
getrandom = { version = "0.2", features = ["js"] }

[lib]
crate-type = ["cdylib", "rlib"]
